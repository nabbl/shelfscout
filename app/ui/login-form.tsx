"use client";
import { useState } from "react";
import { BookOpen, LockKeyhole } from "lucide-react";
export function LoginForm({configured}:{configured:boolean}){const [password,setPassword]=useState("");const [error,setError]=useState("");const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);const r=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({password})});const data=await r.json();if(r.ok)location.reload();else{setError(data.error||"Sign-in failed");setBusy(false);}}return <main className="login-page"><section className="login-card"><div className="login-logo"><BookOpen/> ShelfScout</div><p className="eyebrow">PRIVATE BOOK COMPANION</p><h1>Welcome back to<br/>your reading life.</h1><p>Your history, notes and requests stay behind this owner sign-in.</p>{configured?<form onSubmit={submit}><label>Password<input type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} required/></label>{error&&<p className="form-error">{error}</p>}<button disabled={busy}><LockKeyhole size={16}/>{busy?"Signing in…":"Sign in"}</button></form>:<div className="setup-warning"><strong>Owner sign-in needs setup</strong><span>Set OWNER_PASSWORD_HASH and SESSION_SECRET, then restart ShelfScout.</span></div>}</section></main>}
