import { describe,expect,it } from "vitest";
import { configuredUpstreamUrl,safeExternalUrl,redact } from "../src/lib/security";
import { normalizeStatus } from "../src/lib/acquisition";
describe("security and status boundaries",()=>{it("allows explicitly configured private hosts but rejects credentials and unsafe catalog hosts",()=>{expect(configuredUpstreamUrl("http://192.168.1.8:3000/","BookOrbit").host).toBe("192.168.1.8:3000");expect(()=>configuredUpstreamUrl("http://user:pass@host/","BookOrbit")).toThrow();expect(()=>configuredUpstreamUrl("file:///etc/passwd","BookOrbit")).toThrow();expect(()=>safeExternalUrl("https://127.0.0.1/",["openlibrary.org"])).toThrow();expect(safeExternalUrl("https://openlibrary.org/works/1",["openlibrary.org"])).toContain("openlibrary.org");expect(redact("Authorization=Bearer-secret password=hunter2")).not.toContain("hunter2");});it("never maps early upstream states to ready",()=>{expect(normalizeStatus("pending")).toBe("requested_waiting_approval");expect(normalizeStatus("approved")).toBe("searching_selecting");expect(normalizeStatus("grabbed","downloading")).toBe("downloading");expect(normalizeStatus("available")).toBe("available_in_bookorbit");});});

