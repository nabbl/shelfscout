import { describe,expect,it } from "vitest";
import { rankCandidates } from "../src/lib/recommendations";
import { workKey } from "../src/lib/identity";
describe("recommendation diagnostics",()=>{it("excludes known/dismissed works, diversifies authors, uses negative signals and grounds explanations",()=>{const docs=[{key:"/works/1",title:"Known",author_name:["A"]},{key:"/works/2",title:"Dismissed",author_name:["B"]},{key:"/works/3",title:"Fresh",author_name:["C"],subject:["Memory"]},{key:"/works/4",title:"Same author",author_name:["C"],subject:["Memory"]},{key:"/works/5",title:"Other",author_name:["D"],subject:["History"]}];const result=rankCandidates(docs,new Set([workKey("Known","A")]),new Set([workKey("Dismissed","B")]),["Beloved"],"quiet",["Memory"]);expect(result.map(r=>r.title)).toEqual(["Other","Fresh"]);expect(result[0].why).toContain("Beloved");expect(result[0].why).toContain("quiet");expect(result[0].sourceUrl).toBe("https://openlibrary.org/works/5");expect(result[1].caveat).toContain("lower-rated history");expect(new Set(result.map(r=>r.author)).size).toBe(result.length);expect(result[0].ratings.amazon.rating).toBeNull();});});
